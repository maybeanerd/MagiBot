module.exports = {
    main: (bot, msg) => {
        if (msg.author.id == bot.OWNERID) {
            //TODO

        }
    },
    admin: true,
    help: 'Schicke ein Update an alle Notification Channel.',
    hide: true
};
